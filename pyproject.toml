[project]
name = "codex-workspace-gateway"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.111.1,<0.112",
  "starlette>=0.37.2,<0.38",
  "uvicorn",
  "python-multipart",
  "paramiko",
  "boto3",
  "mcp>=1.27,<2",
  "sse-starlette>=1.6.1,<3",
  "pytest",
]
